const cv = document.createElement('canvas'), cx = canvas.getContext('2d');

// Set canvas width and height
canvas.width = 800;
canvas.height = 600;

// Append the canvas to the document body
document.body.appendChild(canvas);
